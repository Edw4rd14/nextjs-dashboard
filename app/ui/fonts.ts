import { Poppins, Lusitana } from "next/font/google";

export const poppins = Poppins({style:['normal'], weight:['300'], subsets:['latin']});
export const lusitana = Lusitana({subsets:['latin'], weight:['400']});